name: pr

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_TOKEN: ${{ github.action }}
        run: echo "${GITHUB_TOKEN}"
